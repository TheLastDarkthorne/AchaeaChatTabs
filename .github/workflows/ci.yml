name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Muddle
        uses: demonnic/build-with-muddler@latest
      
      - name: Upload Artifact
        uses: actions/upload-artifact@v2
        with:
          name: AchaeaChatTabs.mpackage
          path: build/*.mpackage
